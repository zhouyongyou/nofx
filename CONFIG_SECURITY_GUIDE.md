# 🔐 配置文件安全指南

## 為什麼需要這份指南？

標準 JSON 格式**不支持註釋** (`//`)，但配置文件中的敏感信息（特別是私鑰）需要特別警告。這份文檔補充說明 `config.json` 中每個欄位的安全考量。

---

## 📋 交易所配置安全說明

### 1️⃣ Binance (CEX)

```json
{
  "exchange": "binance",
  "binance_api_key": "YOUR_API_KEY_HERE",
  "binance_secret_key": "YOUR_SECRET_KEY_HERE"
}
```

**安全等級:** 🟡 中等

**欄位說明:**
- `binance_api_key`: Binance API Key
  - ✅ 可在 Binance 後台撤銷
  - ✅ 可設定 IP 白名單
  - ✅ 可限制權限（建議只開啟「交易」權限）
  
- `binance_secret_key`: Binance Secret Key
  - ⚠️ 用於簽名請求，不要洩漏
  - ✅ 洩漏後可立即在後台撤銷

**最佳實踐:**
```bash
✅ 在 Binance 後台創建專用 API Key
✅ 只開啟「期貨交易」權限（關閉「提現」）
✅ 綁定伺服器 IP（如果固定 IP）
✅ 定期輪換 API Key（建議每月一次）
```

---

### 2️⃣ Hyperliquid (DEX)

```json
{
  "exchange": "hyperliquid",
  "hyperliquid_private_key": "your_ethereum_private_key_without_0x",
  "hyperliquid_wallet_addr": "0xYourEthereumAddress",
  "hyperliquid_testnet": false
}
```

**安全等級:** 🔴 高危

**欄位說明:**
- `hyperliquid_private_key`: **以太坊私鑰**（不是 API Key！）
  - 🔴 這是你的完整錢包私鑰
  - ❌ 洩漏 = 失去錢包完全控制權
  - ❌ 無法撤銷！唯一解決方案是創建新錢包
  - ⚠️ 必須移除 `0x` 前綴

- `hyperliquid_wallet_addr`: 以太坊錢包地址（公開地址）
  - ✅ 公開信息，可分享
  - 用於驗證身份

- `hyperliquid_testnet`: 是否使用測試網
  - `false`: 主網（真實資金）
  - `true`: 測試網（測試用）

**最佳實踐:**
```bash
✅ 創建專用錢包（不要用主錢包！）
✅ 只在專用錢包存放交易所需資金（$100-500）
✅ 先在測試網測試（hyperliquid_testnet: true）
✅ 定期檢查錢包交易記錄
❌ 絕對不要將 config.json 提交到 Git
❌ 絕對不要重複使用你的主錢包私鑰
```

**如何創建專用錢包:**
```bash
# 方法 1: 使用 MetaMask
1. 打開 MetaMask → 帳戶 → 創建帳戶
2. 命名為 "Hyperliquid Trading Only"
3. 導出私鑰 → 填入 config.json
4. 只在這個錢包存放交易資金

# 方法 2: 使用離線工具
1. 訪問 https://vanity-eth.tk (離線模式)
2. 生成新錢包
3. 妥善保管私鑰
```

---

### 3️⃣ Aster DEX (混合方案)

```json
{
  "exchange": "aster",
  "aster_user": "0xYourMainWalletAddress",
  "aster_signer": "0xAPIWalletSignerAddress",
  "aster_private_key": "api_wallet_private_key_without_0x"
}
```

**安全等級:** 🟠 中高

**欄位說明:**
- `aster_user`: 你的主錢包地址
  - ✅ 公開信息
  - 用於登入 Aster 平台

- `aster_signer`: API Wallet 地址
  - ✅ 從 Aster 網站創建
  - ✅ 可在後台禁用

- `aster_private_key`: API Wallet 私鑰
  - ⚠️ 仍然是以太坊私鑰（不是傳統 API Key）
  - ✅ 可在 Aster 後台「禁用」（阻止 API 調用）
  - ⚠️ 局限性：禁用不會改變私鑰本身
  - ⚠️ 攻擊者仍可繞過 Aster 直接操作區塊鏈

**最佳實踐:**
```bash
✅ 主錢包存放大額資金（不交易）
✅ API Wallet 只存交易金額（$100-500）
✅ 定期檢查 API Wallet 權限狀態
✅ 如懷疑洩漏，立即在後台禁用
```

**創建 API Wallet:**
```bash
1. 訪問 https://www.asterdex.com/en/api-wallet
2. 連接你的主錢包（MetaMask）
3. 點擊 "Create API Wallet"
4. 保存三個信息：
   - Main Wallet Address (aster_user)
   - API Wallet Signer (aster_signer)
   - API Wallet Private Key (aster_private_key) ⚠️ 只顯示一次！
```

---

## 🛡️ 通用安全建議

### 環境變數方式（推薦）

**為什麼用環境變數？**
- ✅ 不會意外提交到 Git
- ✅ 容易在不同環境切換
- ✅ 雲端部署時更安全

**設定方式:**
```bash
# 1. 創建 .env 文件（記得加入 .gitignore）
cat > .env << 'EOL'
HYPERLIQUID_PRIVATE_KEY=your_private_key_here
BINANCE_API_KEY=your_api_key_here
BINANCE_SECRET_KEY=your_secret_key_here
EOL

# 2. 在 config.json 中使用佔位符
{
  "hyperliquid_private_key": "${HYPERLIQUID_PRIVATE_KEY}"
}

# 3. 程式啟動時載入環境變數
export $(cat .env | xargs)
./nofx
```

---

### 資料庫加密（強烈建議）

**目前狀態:**
- ⚠️ 私鑰以明文存在 SQLite 資料庫
- ⚠️ 任何能訪問 `config.db` 的人都能讀取

**啟用加密:**
```bash
# 參閱 docs/ENCRYPTION_DEPLOYMENT.md
./deploy_encryption.sh
```

---

### 檢查清單

在部署前，請確認：

- [ ] 所有私鑰使用專用錢包（不是主錢包）
- [ ] 專用錢包只存放必要資金
- [ ] `config.json` 已加入 `.gitignore`
- [ ] `.env` 文件已加入 `.gitignore`
- [ ] 已在測試網測試配置
- [ ] 已啟用資料庫加密
- [ ] 定期檢查錢包交易記錄

---

## 🚨 如果私鑰洩漏了怎麼辦？

### Binance
```bash
1. 立即登入 Binance
2. API 管理 → 刪除洩漏的 API Key
3. 創建新的 API Key
4. 更新 config.json
```

### Hyperliquid
```bash
⚠️ 無法撤銷！必須：
1. 創建新的以太坊錢包
2. 將舊錢包資產轉移到新錢包
3. 更新 config.json 使用新錢包私鑰
4. 廢棄舊錢包
```

### Aster
```bash
1. 立即訪問 https://www.asterdex.com/en/api-wallet
2. 禁用被洩漏的 API Wallet
3. 創建新的 API Wallet
4. 更新 config.json
5. ⚠️ 如果攻擊者已經竊取資金，無法追回
```

---

## 📖 延伸閱讀

- [README.md](README.md) - 完整配置說明
- [ENCRYPTION_DEPLOYMENT.md](docs/ENCRYPTION_DEPLOYMENT.md) - 加密系統部署
- [SECURITY.md](SECURITY.md) - 安全政策
- [Hyperliquid 官方文檔](https://hyperliquid.gitbook.io)
- [Aster API 文檔](https://docs.asterdex.com)

---

**記住：私鑰就是你的資產。保護私鑰 = 保護財產。**
